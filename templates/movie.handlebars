<article class="movieSite">
  <h1 class="movieTitle">{{ movie.attributes.title }}</h1>
  <div class="movieTitle">
    {{{ movie.attributes.intro }}}
  </div>
  {{#if movie.attributes.image }}
  <img src="{{ movie.attributes.image.url }}">
  {{/if }}
</article>